server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health, info

spring:
  application:
    name: spring-boot-admin
  boot:
    admin:
      discovery:
        enabled: true